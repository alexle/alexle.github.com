#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run both HTML and SCSS linters on staged files
echo "Running linters..."
npm run lint || (echo "Linting failed. Please fix HTML and SCSS issues before committing." && exit 1)
